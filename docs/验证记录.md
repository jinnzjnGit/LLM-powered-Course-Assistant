# 验证记录

验证日期：2026年9月5日。环境：Windows、Python 3.12.14、Streamlit 1.62.0。完整依赖版本见requirements-lock.txt。

| 检查 | 结果 |
|---|---|
| 本机虚拟环境解释器修复 | 可运行，应用依赖均可导入 |
| scripts/check_environment.py | 通过；仅确认模型配置已填写，不确认远程服务可用 |
| pip check | No broken requirements found |
| unittest discover -s tests -v | 31项通过，0失败，0跳过 |
| Python compileall | 通过 |
| setup.ps1、start.ps1语法解析 | 通过 |

回归测试覆盖：Python缩进、分段边界、检索来源、文件内容签名、题目字段与数量、重复题干、评分完整性与范围、引用编号、上下文限制、答案快照、资料绑定、重复保存、历史表备份迁移、计划读写、PDF文本层提取、OCR分支及异常释放、页面提交与重新批改。

页面测试使用Streamlit AppTest，执行生成试卷、提交、AI批改、切换文件、保存、修改答案、重新批改和再次保存。它不是浏览器截图或视觉验收。

测试使用临时数据库，未读取或写入真实学生答案；模型与OCR返回使用替身。真实模型的连通性、答案质量、扫描件OCR准确率、大文件性能、共享部署并发和干净机器安装仍需对应实测，不能由本报告推断通过。

## 复现

在项目目录依次运行：

```powershell
.\.venv\Scripts\python.exe scripts/check_environment.py
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

如果应用依赖未安装，集成测试会显示skipped；这种结果不等于本报告的31项全通过。首次实际启动旧数据库时自动执行备份和迁移，建议启动前先关闭此前运行的旧版应用。
